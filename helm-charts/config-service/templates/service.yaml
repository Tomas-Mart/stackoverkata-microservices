apiVersion: v1
kind: Service
metadata:
  name: stackover-config-service
  namespace: config-server
  labels:
    app: config-service
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: config-service
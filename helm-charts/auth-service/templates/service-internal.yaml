apiVersion: v1
kind: Service
metadata:
  name: stackover-auth-service-internal
  labels:
    purpose: internal
spec:
  type: ClusterIP
  ports:
    - port: 8180
      targetPort: 8180
      protocol: TCP
  selector:
    app: auth-service
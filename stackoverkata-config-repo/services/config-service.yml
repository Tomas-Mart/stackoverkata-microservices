spring:
  application:
    name: stackoverkata-cloud-config-service
  cloud:
    config:
      server:
        git:
          uri: ${CONFIG_REPO_URL}
          search-paths: '{application}'
          clone-on-start: true

server:
  port: 8888

management:
  endpoint:
    health:
      show-details: always